# 初期メッセージoff
startup_message off
# 起動時にbashで開始
shell -bash
# キーバインドをCtrl+zに変更
escape ^z^z
# ステータスラインを常に表示
hardstatus alwayslastline "%{.bW}%-w%{.rW}%n %t%{-}%+w %=%{..G} %H %{..Y} %Y %m/%d %C%a "
hardstatus off

# ビジュアルベルをoff
vbell off
